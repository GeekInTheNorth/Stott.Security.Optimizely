@using EPiServer.Web.Mvc.Html
@model OptimizelyTwelveTest.Features.Home.HomePageViewModel

<div>
    @Html.PropertyFor(x => x.CurrentPage.HeroImage)
    <h1>@Html.PropertyFor(x => x.CurrentPage.Heading)</h1>
    <div>
        @Html.PropertyFor(x => x.CurrentPage.MainContentArea)
    </div>
    <iframe src="https://google.com" width="300" height="300"></iframe>
</div>

@await Component.InvokeAsync("CspReporting")